include(CMakeFindDependencyMacro)

find_dependency(glbinding QUIET)
find_dependency(globjects QUIET)

if(@OpenCL_FOUND@)
    find_dependency(OpenCL QUIET)
endif()

if(@Vulkan_FOUND@)
    find_dependency(Vulkan QUIET)
endif()

if(@cereal_FOUND@)
    find_dependency(cereal QUIET)
endif()

include("${CMAKE_CURRENT_LIST_DIR}/VisionCoreTargets.cmake")
